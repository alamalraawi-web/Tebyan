# منظومة الطبيب في تبيان

## التشغيل
```bash
npm install
npm run dev
```

## ما تم تعديله
- صفحات الطبيب فقط.
- تسجيل الدخول وإنشاء الحساب.
- نظام حفظ الحسابات والجلسات والتوجيه بحسب الدور.
- حماية مسارات الطبيب من الحسابات الأخرى.

## الحسابات
كل حساب جديد يُحفظ في `tebyan-users`، والجلسة الحالية في `tebyan-session`.
- الطبيب: `/doctor/dashboard`
- الصيدلي: `/main/pharmacy`
- المريض وبقية الأدوار الحالية: `/home`

## صفحات الطبيب
`dashboard`, `consultations`, `patients`, `appointments`, `diseases`, `messages`, `notifications`, `settings`.

البيانات التجريبية الخاصة بكل طبيب تُحفظ محليًا بشكل منفصل بحسب معرّف حسابه.
