import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { loadDoctorData } from '../../doctor/doctorStore';
export default function DoctorPatients(){const nav=useNavigate();const [q,setQ]=useState('');const rows=loadDoctorData('patients').filter(p=>(p.name+p.condition+p.phone).includes(q));return <><div className="page-head"><div><h1>المرضى</h1><p>سجل المرضى المرتبطين باستشاراتك فقط.</p></div></div><div className="toolbar"><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث بالاسم أو رقم الجوال أو الحالة"/></div><div className="panel table-wrap"><table className="doctor-table"><thead><tr><th>المريض</th><th>العمر</th><th>الجنس</th><th>آخر حالة</th><th>الزيارات</th><th>الإجراء</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td><strong>{p.name}</strong></td><td>{p.age}</td><td>{p.gender}</td><td>{p.condition}</td><td>{p.visits}</td><td><button className="btn btn-soft" onClick={()=>nav(`/doctor/patients/${p.id}`)}>عرض الملف</button></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">لا توجد نتائج.</div>}</div></>}
