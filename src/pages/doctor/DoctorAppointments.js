import { useState } from 'react';import { loadDoctorData,saveDoctorData } from '../../doctor/doctorStore';
export default function DoctorAppointments(){const [rows,setRows]=useState(loadDoctorData('appointments'));const [open,setOpen]=useState(false);const [toast,setToast]=useState('');function save(next,msg){setRows(next);saveDoctorData('appointments',next);setToast(msg);setTimeout(()=>setToast(''),1600)}function add(e){e.preventDefault();const f=new FormData(e.currentTarget);save([...rows,{id:`A-${Date.now()}`,date:f.get('date'),time:f.get('time'),patient:f.get('patient'),type:f.get('type'),status:'confirmed'}],'تمت إضافة الموعد');setOpen(false)}function cancel(id){save(rows.map(x=>x.id===id?{...x,status:'cancelled'}:x),'تم إلغاء الموعد')}return <><div className="page-head"><div><h1>المواعيد</h1><p>إدارة جدولك اليومي وإضافة مواعيد جديدة.</p></div><button className="btn btn-primary" onClick={()=>setOpen(true)}>إضافة موعد</button></div><div className="panel"><div className="list">{rows.sort((a,b)=>(a.date+a.time).localeCompare(b.date+b.time)).map(a=><div className="list-card" key={a.id}><div className="doctor-avatar">{a.time}</div><div className="grow"><h3>{a.patient}</h3><p>{a.date} · {a.type}</p></div><span className={`badge ${a.status}`}>{a.status==='confirmed'?'مؤكد':a.status==='cancelled'?'ملغي':'انتظار'}</span>{a.status!=='cancelled'&&<button className="btn btn-danger" onClick={()=>cancel(a.id)}>إلغاء</button>}</div>)}</div></div>{open&&<div className="modal-backdrop"><form className="modal" onSubmit={add}><div className="modal-head"><h2>موعد جديد</h2><button type="button" className="btn btn-soft" onClick={()=>setOpen(false)}>إغلاق</button></div><div className="form-grid"><div className="field full"><label>اسم المريض</label><input className="input" name="patient" required/></div><div className="field"><label>التاريخ</label><input className="input" name="date" type="date" required/></div><div className="field"><label>الوقت</label><input className="input" name="time" type="time" required/></div><div className="field full"><label>نوع الموعد</label><select className="select" name="type"><option>استشارة فيديو</option><option>استشارة نصية</option><option>متابعة علاج</option></select></div><button className="btn btn-primary">حفظ الموعد</button></div></form></div>}{toast&&<div className="toast">{toast}</div>}</>}
