import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { loadDoctorData } from '../../doctor/doctorStore';
const labels={new:'جديدة',in_progress:'قيد المتابعة',waiting:'بانتظار المريض',completed:'مكتملة'};
export default function DoctorConsultations(){const nav=useNavigate();const [q,setQ]=useState('');const [status,setStatus]=useState('all');const rows=loadDoctorData('consultations').filter(x=>(x.patient+x.topic).includes(q)&&(status==='all'||x.status===status));return <><div className="page-head"><div><h1>الاستشارات</h1><p>استقبل الحالات، تابعها، وأغلقها بخطوات واضحة.</p></div></div><div className="toolbar"><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث باسم المريض أو موضوع الحالة"/><select className="select" value={status} onChange={e=>setStatus(e.target.value)}><option value="all">كل الحالات</option>{Object.entries(labels).map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></div><div className="panel table-wrap"><table className="doctor-table"><thead><tr><th>الحالة</th><th>المريض</th><th>العمر</th><th>الموضوع</th><th>الوضع</th><th>الإجراء</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.id}</td><td><strong>{x.patient}</strong></td><td>{x.age} سنة</td><td>{x.topic}</td><td><span className={`badge ${x.priority==='urgent'?'urgent':x.status}`}>{x.priority==='urgent'?'عاجلة':labels[x.status]}</span></td><td><button className="btn btn-soft" onClick={()=>nav(`/doctor/consultations/${x.id}`)}>فتح الحالة</button></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">لا توجد نتائج مطابقة.</div>}</div></>}
